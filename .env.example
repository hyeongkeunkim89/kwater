# ── 물 이야기(프로덕션) ─────────────────────────────────────────
# Neon / Supabase / Vercel Postgres 등 PostgreSQL 연결 문자열
DATABASE_URL=

# Vercel Blob — 프로젝트 Storage에서 Read/Write 토큰 발급
BLOB_READ_WRITE_TOKEN=

# 갤러리 관리(이달의 사진 지정·글 삭제) 시 API에 전달하는 비밀값
WATER_STORIES_ADMIN_SECRET=
